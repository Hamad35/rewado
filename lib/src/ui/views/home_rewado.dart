import 'package:flutter/material.dart';
import '../../utils/config/uidata.dart';

class Home_rewado extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: UIDataColors.commonColor,
      ),
    );
  }
}
